import { MorsePipePipe } from './morse-MD5-pipe.pipe';

describe('MorsePipePipe', () => {
  it('create an instance', () => {
    const pipe = new MorsePipePipe();
    expect(pipe).toBeTruthy();
  });
});
